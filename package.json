{
  "dependencies": {
    "aws-sdk": "^2.327.0",
    "bluebird": "^3.5.0",
    "lodash": "^4.17.4",
    "proxy-agent": "^2.0.0",
    "uuid": "^3.3.2"
  },
  "name": "@sparkpost/aws",
  "description": "Thin wrappers around the aws-sdk libraries we use.",
  "version": "1.12.0",
  "main": "index.js",
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^7.0.0",
    "chai-sinon": "^2.8.1",
    "eslint": "^4.3.0",
    "eslint-config-sparkpost": "^1.3.2",
    "mocha": "^3.4.2",
    "nyc": "^11.0.2",
    "proxyquire": "^1.8.0",
    "sinon": "^2.4.1",
    "xunit-file": "^1.0.0"
  },
  "scripts": {
    "lint": "eslint --fix index.js lib/**/*.js test/unit/**/*.js",
    "coverage-report": "nyc report",
    "test-base": "nyc --reporter=lcov --report-dir=test/reports/unit mocha test/unit/",
    "test": "npm run lint && npm run test-base -- --reporter xunit-file && npm run coverage-report",
    "dev-validate": "npm run lint && npm run test-base && npm run coverage-report"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SparkPost/aws.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/SparkPost/aws/issues"
  },
  "homepage": "https://github.com/SparkPost/aws#readme"
}
